# Screenshots

The dashboard currently ships as a lightweight shell. Replace these placeholders with real screenshots before a public release announcement.

## Dashboard landing page

Expected screenshot path once captured:

```text
docs/assets/dashboard-home.png
```

Capture command after running the stack:

```bash
docker compose up --build
# Open http://localhost:3000 and capture the dashboard landing page.
```

## API documentation

Expected screenshot path once captured:

```text
docs/assets/api-docs.png
```

Capture command after running the stack:

```bash
# Open http://localhost:8080/docs and capture the OpenAPI documentation page.
```
